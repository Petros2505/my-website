name: Build and Deploy to GKE

on:
  push:
    branches: [ "master" ]

jobs:
  setup-build-publish-deploy:
    name: Setup, Build, Publish, and Deploy
    runs-on: ubuntu-latest
    environment: production

    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Authenticate to Google Cloud
      uses: google-github-actions/auth@v1
      with:
        credentials_json: ${{ secrets.GCP_SA_KEY }}
        export_default_credentials: true

    - name: Build Docker image
      run: |
        gcloud auth configure-docker europe-central2-a.pkg.dev
        docker images
        docker build -t europe-central2-a.pkg.dev/kinetic-object-413113/my-img:row .
        docker push europe-central2-a.pkg.dev/kinetic-object-413113/gcr.io/my-img:row
        
#        docker tag europe-central2-a.pkg.dev/kinetic-object-413113/my-img:row eu-central2-a.pkg.dev/kinetic-object-413113/my-img:why

